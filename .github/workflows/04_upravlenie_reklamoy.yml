name: TOPFACE WB Ads Manager

on:
  workflow_dispatch:
    inputs:
      run_slot:
        description: "Что запустить вручную"
        required: true
        default: "main"
        type: choice
        options:
          - main
          - night_start
          - night_end
      dry_run:
        description: "Dry-run: рассчитать без отправки в WB"
        required: true
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

  schedule:
    # GitHub Actions cron работает в UTC. Москва = UTC+3.
    - cron: "0 22 * * *"  # 01:00 МСК — Эксперимент 1: минимальные ставки; Эксперимент 2: pause combined с ДРР > 15%
    - cron: "0 2 * * *"   # 05:00 МСК — Эксперимент 2: включить обратно ночные паузы
    - cron: "0 11 * * *"  # 14:00 МСК — основной дневной запуск алгоритма

concurrency:
  group: topface-wb-ads-manager
  cancel-in-progress: false

jobs:
  run-topface-ads-manager:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    env:
      # Можно использовать отдельные GitHub Secrets или общий многострочный REPORT_ENV.
      REPORT_ENV: ${{ secrets.REPORT_ENV }}
      YC_ACCESS_KEY_ID: ${{ secrets.YC_ACCESS_KEY_ID }}
      YC_SECRET_ACCESS_KEY: ${{ secrets.YC_SECRET_ACCESS_KEY }}
      YC_BUCKET_NAME: ${{ secrets.YC_BUCKET_NAME }}
      WB_PROMO_KEY_TOPFACE: ${{ secrets.WB_PROMO_KEY_TOPFACE }}

      PYTHONUNBUFFERED: "1"
      TZ: "Europe/Moscow"

      # Явно фиксируем ночное окно для кода.
      WB_NIGHT_EXPERIMENTS_ENABLED: "1"
      WB_NIGHT_EXPERIMENT_START_HOUR_MSK: "1"
      WB_NIGHT_EXPERIMENT_END_HOUR_MSK: "5"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          python -m pip install pandas openpyxl xlsxwriter requests boto3 python-dateutil numpy pytz

      - name: Load secrets from REPORT_ENV
        run: |
          set -euo pipefail
          python - <<'PY'
          import os

          allowed = [
              "YC_ACCESS_KEY_ID",
              "YC_SECRET_ACCESS_KEY",
              "YC_BUCKET_NAME",
              "WB_PROMO_KEY_TOPFACE",
          ]

          report_env = os.getenv("REPORT_ENV", "") or ""
          parsed = {}
          for raw_line in report_env.splitlines():
              line = raw_line.strip()
              if not line or line.startswith("#"):
                  continue
              if line.startswith("export "):
                  line = line[len("export "):].strip()
              if "=" not in line:
                  continue
              key, value = line.split("=", 1)
              key = key.strip()
              value = value.strip().strip('"').strip("'")
              if key in allowed and value:
                  parsed[key] = value

          github_env = os.environ.get("GITHUB_ENV")
          if not github_env:
              raise SystemExit("GITHUB_ENV is not available")

          loaded = []
          with open(github_env, "a", encoding="utf-8") as f:
              for key in allowed:
                  value = os.getenv(key) or parsed.get(key)
                  if value:
                      f.write(f"{key}={value}\n")
                      loaded.append(key)

          print("Loaded secret names: " + ", ".join(loaded))
          PY

      - name: Verify required secrets
        run: |
          set -euo pipefail
          python - <<'PY'
          import os
          required = [
              "YC_ACCESS_KEY_ID",
              "YC_SECRET_ACCESS_KEY",
              "YC_BUCKET_NAME",
              "WB_PROMO_KEY_TOPFACE",
          ]
          missing = [name for name in required if not os.getenv(name)]
          if missing:
              raise SystemExit("Missing secrets: " + ", ".join(missing))
          print("All required secrets are present.")
          PY

      - name: Resolve and verify script
        run: |
          set -euo pipefail
          echo "=== Time diagnostics ==="
          echo "UTC: $(date -u '+%Y-%m-%d %H:%M:%S')"
          echo "MSK: $(TZ=Europe/Moscow date '+%Y-%m-%d %H:%M:%S')"

          echo "=== Current directory ==="
          pwd
          echo "=== Repository files ==="
          ls -la

          SCRIPT_PATH=""
          if [ -f "upravlenie_reklamoy.py" ]; then
            SCRIPT_PATH="upravlenie_reklamoy.py"
          elif [ -f "assistant_wb_ads_manager.py" ]; then
            SCRIPT_PATH="assistant_wb_ads_manager.py"
          else
            echo "ERROR: не найден файл скрипта: ожидал ./upravlenie_reklamoy.py или ./assistant_wb_ads_manager.py"
            exit 1
          fi

          echo "SCRIPT_PATH=$SCRIPT_PATH" >> "$GITHUB_ENV"
          export SCRIPT_PATH
          echo "Resolved script: $SCRIPT_PATH"

          echo "=== Script version marker ==="
          grep -n "SCRIPT_VERSION" "$SCRIPT_PATH" || true

          echo "=== Old dayfirst bug check ==="
          if grep -q "dayfirst=True" "$SCRIPT_PATH"; then
            echo "ERROR: В $SCRIPT_PATH есть старый dayfirst=True"
            exit 1
          fi

          echo "=== Required code markers ==="
          python - <<'PY'
          import os
          import re
          from pathlib import Path

          script_path = os.environ.get("SCRIPT_PATH")
          if not script_path:
              for candidate in ("upravlenie_reklamoy.py", "assistant_wb_ads_manager.py"):
                  if Path(candidate).exists():
                      script_path = candidate
                      break
          if not script_path:
              raise SystemExit("ERROR: SCRIPT_PATH is empty and script file was not found")
          text = Path(script_path).read_text(encoding="utf-8", errors="replace")

          m = re.search(r'SCRIPT_VERSION\s*=\s*["\']([^"\']+)["\']', text)
          if not m:
              raise SystemExit(f"ERROR: В {script_path} нет SCRIPT_VERSION")
          version = m.group(1)
          print(f"SCRIPT_VERSION={version}")

          vm = re.search(r'strict-drr-v(\d+)', version)
          if not vm:
              raise SystemExit(f"ERROR: Неизвестный формат SCRIPT_VERSION: {version}")
          vnum = int(vm.group(1))
          if vnum < 32:
              raise SystemExit(f"ERROR: В {script_path} версия v{vnum}, нужна v32+ с принудительным ночным слотом")

          markers = [
              "night_experiment_only",
              "EXPERIMENT_1_NIGHT_MIN_BID_MSK_1_5",
              "EXPERIMENT_2_NIGHT_COMBINED_DRR_GT_15_PAUSE",
              "ABC_PROFITABILITY_MIN_PCT",
              "abc_profitability_pct",
              "night_experiment_slot",
          ]
          missing = [marker for marker in markers if marker not in text]
          if missing:
              raise SystemExit("ERROR: В коде нет обязательных маркеров: " + ", ".join(missing))
          print("Code marker check OK: v32+, forced night slot, night experiments, ABC profitability present.")
          PY

          echo "=== Script SHA256 ==="
          sha256sum "$SCRIPT_PATH"

      - name: Compile Python script
        run: |
          set -euo pipefail
          python -m py_compile "$SCRIPT_PATH"

      - name: Run TOPFACE WB Ads Manager
        env:
          EVENT_NAME: ${{ github.event_name }}
          SCHEDULE_CRON: ${{ github.event.schedule || '' }}
          INPUT_RUN_SLOT: ${{ github.event.inputs.run_slot || 'main' }}
          INPUT_DRY_RUN: ${{ github.event.inputs.dry_run || 'false' }}
        run: |
          set -euo pipefail

          BASE_CMD="python $SCRIPT_PATH run --skip-price"
          SLOT="main"

          if [ "$EVENT_NAME" = "schedule" ]; then
            case "$SCHEDULE_CRON" in
              "0 22 * * *")
                SLOT="night_start"
                CMD="$BASE_CMD --night-experiment-only --night-experiment-slot start --apply-pause"
                ;;
              "0 2 * * *")
                SLOT="night_end"
                CMD="$BASE_CMD --night-experiment-only --night-experiment-slot end --apply-start"
                ;;
              "0 11 * * *")
                SLOT="main"
                CMD="$BASE_CMD"
                ;;
              *)
                echo "ERROR: неизвестное расписание: $SCHEDULE_CRON"
                exit 1
                ;;
            esac
          else
            case "$INPUT_RUN_SLOT" in
              "night_start")
                SLOT="night_start"
                CMD="$BASE_CMD --night-experiment-only --night-experiment-slot start --apply-pause"
                ;;
              "night_end")
                SLOT="night_end"
                CMD="$BASE_CMD --night-experiment-only --night-experiment-slot end --apply-start"
                ;;
              "main"|"")
                SLOT="main"
                CMD="$BASE_CMD"
                ;;
              *)
                echo "ERROR: неизвестный run_slot: $INPUT_RUN_SLOT"
                exit 1
                ;;
            esac
          fi

          if [ "$INPUT_DRY_RUN" = "true" ]; then
            CMD="$CMD --dry-run"
          fi

          echo "=== Run slot diagnostics ==="
          echo "Event: $EVENT_NAME"
          echo "Schedule cron: ${SCHEDULE_CRON:-manual}"
          echo "Manual input slot: ${INPUT_RUN_SLOT:-none}"
          echo "Resolved slot: $SLOT"
          echo "Resolved script: $SCRIPT_PATH"
          echo "UTC now: $(date -u '+%Y-%m-%d %H:%M:%S')"
          echo "MSK now: $(TZ=Europe/Moscow date '+%Y-%m-%d %H:%M:%S')"
          echo "Running: $CMD"

          eval "$CMD"

      - name: Show local result files
        if: always()
        run: |
          echo "=== Local XLSX/JSON files after run ==="
          find . -maxdepth 4 -type f \( -name "*.xlsx" -o -name "*.json" \) -print || true

      - name: Upload local artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: topface-ads-manager-results
          path: |
            *.xlsx
            *.json
            **/Итог_последнего_запуска*.xlsx
            **/Предпросмотр_последнего_запуска*.xlsx
            **/Сводка_последнего_запуска*.json
            **/Лог_API*.xlsx
            **/История_ставок*.xlsx
            **/История_пауз*.xlsx
          if-no-files-found: warn
          retention-days: 14
